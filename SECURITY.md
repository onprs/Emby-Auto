# 安全策略

**简体中文** | [English](SECURITY.en.md)

## 支持范围

Emby Auto 当前处于 `0.x` 开发阶段。安全修复仅面向默认分支的最新版本。

| 版本范围 | 安全支持 |
| --- | --- |
| `main` 最新提交 | 支持 |
| 旧提交、第三方分支与非官方镜像 | 不支持 |

部署者应在完成备份和兼容性检查后及时升级。第三方组件自身的漏洞应报告给对应上游项目；由 Emby Auto 集成方式引入的问题属于本策略范围。

## 私密报告漏洞

应优先使用 GitHub 仓库 **Security** 页面中的 **Report a vulnerability** 提交私密报告。如果该功能不可用，应通过维护者 GitHub 资料中提供的私密联系方式请求安全沟通渠道，首次联系不得包含漏洞细节。

以下内容不得发布到公开 Issue、Discussion、Pull Request、日志或截图：

- 管理员密码、Session Cookie、API Token、数据库连接或配置加密材料。
- 私有 RSS 地址、代理配置、真实媒体信息或内部文件路径。
- 可直接利用的请求、完整攻击链或生产基础设施信息。

## 报告内容

有效报告应尽可能包含：

- 受影响的版本或提交。
- 受影响组件及部署方式。
- 使用脱敏占位值的最小复现步骤。
- 实际行为、预期行为和潜在影响。
- 已验证的临时缓解措施。
- 测试是否在自有或已授权环境中完成。

未经明确授权，不得测试第三方 Emby Auto 实例、外部服务账户或基础设施。

## 处理与披露

维护者会确认报告、评估影响、准备修复并协调披露。报告者应在修复发布或双方约定的披露时间之前保持技术细节私密。

安全修复可能同时更新应用代码、数据库迁移、容器镜像或部署模板。公告将说明受影响范围、升级要求和可用缓解措施。

## 部署安全责任

Emby Auto 是自托管软件。部署者负责：

- TLS 终止、网络访问控制、防火墙和主机安全更新。
- PostgreSQL、安装配置和媒体数据的加密备份。
- 管理员账户与 qBittorrent、TMDb、Emby、Agent 等外部服务凭据。
- 使用非 root 服务账户，并限制媒体目录权限。
- 禁止应用访问 Docker socket、宿主根目录、systemd 总线或非必要 Linux capabilities。

生产部署应遵循 [Docker Compose 部署指南](docs/deployment.md) 或 [Linux 直接部署指南](docs/direct-deployment.md) 中的安全要求。
